{% extends WebHemiApplication.selectedModule == 'Admin' ? 'admin-layout.twig' : 'layout.twig' %}

{% block content %}
    <h1 class="page-header">Forbidden!</h1>
    {# @var exception \Exception #}
    <p>{{ exception.getMessage() }}</p>
{% endblock %}
