<fieldset name="{{ formElement.getName() }}" id="{{ formElement.getId() }}" {{ _FORM.renderFormElementAttributes(formElement.getAttributes()) }}>
    {% if formElement.hasChildNodes() %}
        {{ _FORM.renderSubForm(formElement.getChildNodes()) }}
    {% endif %}
</fieldset>
