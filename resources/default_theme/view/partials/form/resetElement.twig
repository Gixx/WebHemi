{% import '@WebHemi/macros/renderForm.twig' as _FORM %}
<div class="element button reset">
    {% set value = formElement.getValues() %}
    <button type="reset" name="{{ formElement.getName() }}" value="{{ value[0] }}">{{ formElement.getLabel() }}</button>
</div>
