{% import '@WebHemi/macros/renderForm.twig' as _FORM %}
<div class="element input password{% if formElement.getErrors() %} has-error{% endif %}">
    <label for="{{ formElement.getId() }}">{{ formElement.getLabel() }}</label>
    <input type="password" autocomplete="current-password" id="{{ formElement.getId() }}" name="{{ formElement.getName() }}" />
    {% if formElement.getErrors() %}
        {{ _FORM.renderErrorBlock(formElement.getErrors()) }}
    {% endif %}
</div>
