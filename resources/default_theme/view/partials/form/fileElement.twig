{% import '@WebHemi/macros/renderForm.twig' as _FORM %}
<div class="element file{% if formElement.hasErrors() %} error{% endif %}">
    <label for="{{ formElement.getId() }}">{{ formElement.getLabel() }}</label>
    <input type="file" id="{{ formElement.getId() }}" name="{{ formElement.getName() }}"{{ _FORM.renderFormElementAttributes(formElement.getAttributes()) }} />
    {% if formElement.hasErrors() %}
        {{ _FORM.renderErrorBlock(formElement) }}
    {% endif %}
</div>
