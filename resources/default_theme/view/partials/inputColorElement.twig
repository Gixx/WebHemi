<div class="element input color">
    <label for="{{ formElement.getId() }}">{{ formElement.getLabel() }}</label>
    <input type="color" id="{{ formElement.getId() }}" name="{{ formElement.getName() }}" value="{{ formElement.getValue() }}"{{ _FORM.renderFormElementAttributes(formElement.getAttributes()) }} />
</div>
