<div class="element input number">
    <label for="{{ formElement.getId() }}">{{ formElement.getLabel() }}</label>
    <input type="number" id="{{ formElement.getId() }}" name="{{ formElement.getName() }}" value="{{ formElement.getValue() }}"{{ _FORM.renderFormElementAttributes(formElement.getAttributes()) }} />
</div>
